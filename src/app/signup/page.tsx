import Link from "next/link";import { signup } from "./actions";
export default async function Signup({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="min-h-screen grid place-items-center p-6"><div className="w-full max-w-md bg-white border border-[var(--line)] rounded-2xl p-8"><p className="text-sm font-semibold text-[var(--brand)]">GETDATAREADY</p><h1 className="text-3xl font-bold mt-2">Start from the foundations</h1><p className="text-[var(--muted)] mt-2">Create your learner account and we'll track your evidence as you progress.</p>{p.error&&<p className="mt-4 text-sm bg-red-50 p-3 rounded-lg">{p.error}</p>}<form action={signup} className="mt-6 space-y-4"><input name="full_name" required placeholder="Full name" className="w-full border rounded-xl p-3"/><input name="email" type="email" required placeholder="Email" className="w-full border rounded-xl p-3"/><input name="password" type="password" minLength={8} required placeholder="Password (8+ characters)" className="w-full border rounded-xl p-3"/><button className="w-full bg-[var(--brand)] text-white rounded-xl p-3 font-semibold">Create account</button></form><p className="text-sm mt-5 text-center">Already registered? <Link className="text-[var(--brand)] font-semibold" href="/login">Sign in</Link></p></div></main>}
