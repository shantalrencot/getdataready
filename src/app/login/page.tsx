import Link from "next/link";import { login } from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="min-h-screen grid place-items-center p-6"><div className="w-full max-w-md bg-white border border-[var(--line)] rounded-2xl p-8"><p className="text-sm font-semibold text-[var(--brand)]">GETDATAREADY</p><h1 className="text-3xl font-bold mt-2">Welcome back</h1><p className="text-[var(--muted)] mt-2">Continue building proof of your skills.</p>{p.error&&<p className="mt-4 text-sm bg-red-50 p-3 rounded-lg">{p.error}</p>}<form action={login} className="mt-6 space-y-4"><input name="email" type="email" required placeholder="Email" className="w-full border rounded-xl p-3"/><input name="password" type="password" required placeholder="Password" className="w-full border rounded-xl p-3"/><button className="w-full bg-[var(--brand)] text-white rounded-xl p-3 font-semibold">Sign in</button></form><p className="text-sm mt-5 text-center">New learner? <Link className="text-[var(--brand)] font-semibold" href="/signup">Create account</Link></p></div></main>}
